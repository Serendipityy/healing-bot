# Healing Bot Backend Requirements
# For backend-only deployment, install the main requirements file:
# pip install -r ../requirements.txt

# Or use this file for backend-specific dependencies only:
-r ../requirements.txt
