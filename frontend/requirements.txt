# Healing Bot Frontend Requirements
# For frontend-only deployment, install the main requirements file:
# pip install -r ../requirements.txt

# Or use this file for frontend-specific dependencies only:
-r ../requirements.txt
